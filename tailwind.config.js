/** @type {import('tailwindcss').Config} */
// With Tailwind CSS v4, this file is optional if using the CSS-first configuration.
// We are keeping it minimal or can be removed if strictly using @theme in CSS.
export default {
    content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
    theme: {
        extend: {},
    },
    plugins: [],
};
